{
  "outputDirectory": "dist",
  "cleanUrls": true,
  "buildCommand": "npm run build && mkdir -p dist/pricing dist/checkout dist/dashboard dist/account dist/support/article/loading && for d in pricing checkout dashboard account support support/article/loading; do cp dist/index.html dist/$d/index.html; done",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-UX-Fixture",
          "value": "clarity-defect-analysis"
        }
      ]
    }
  ]
}
